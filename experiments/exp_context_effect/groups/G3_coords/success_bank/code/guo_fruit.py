# guo_fruit.py — 果 (guǒ) — B11 curator promotion (2026-08-03)
# Source: groups/G3_coords/attempts/p3_char_0387_果/generated.py
# Strokes: 8
#
# Recipe: 田 upper + 木 lower sharing central 竖 (fresh guo_mu_under_tian; BANK_DEVIATION from mu.py)
#
# v8 signature freedom — this file preserves the recipe pointer;
# the full drawing code lives in the source attempt above. To reuse:
#
#   from pathlib import Path
#   ATTEMPT = Path(__file__).parents[2] / "attempts" / "p3_char_0387_果" / "generated.py"
#   exec(ATTEMPT.read_text())
#
# Or copy the draw block into a new function under a fresh (ox, oy,
# scale) or richer signature per the composition's needs.
