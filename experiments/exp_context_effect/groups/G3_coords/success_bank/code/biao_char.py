# biao_char.py — 表 (biǎo) — B11 curator promotion (2026-08-03)
# Source: groups/G3_coords/attempts/p3_char_0391_表/generated.py
# Strokes: 8
#
# Recipe: 龶-top (3 hengs + shu) + 衣-bottom (撇+竖提+撇+捺); fully inline (BANK_DEVIATION — no 衣/表 primitive)
#
# v8 signature freedom — this file preserves the recipe pointer;
# the full drawing code lives in the source attempt above. To reuse:
#
#   from pathlib import Path
#   ATTEMPT = Path(__file__).parents[2] / "attempts" / "p3_char_0391_表" / "generated.py"
#   exec(ATTEMPT.read_text())
#
# Or copy the draw block into a new function under a fresh (ox, oy,
# scale) or richer signature per the composition's needs.
