# you_help.py — 侑 (yòu) — B11 curator promotion (2026-08-03)
# Source: groups/G3_coords/attempts/p3_char_0424_侑/generated.py
# Strokes: 8
#
# Recipe: 亻 left (bank ren_pang) + 有 right; see you_have_for_LR_right variant (row 247)
#
# v8 signature freedom — this file preserves the recipe pointer;
# the full drawing code lives in the source attempt above. To reuse:
#
#   from pathlib import Path
#   ATTEMPT = Path(__file__).parents[2] / "attempts" / "p3_char_0424_侑" / "generated.py"
#   exec(ATTEMPT.read_text())
#
# Or copy the draw block into a new function under a fresh (ox, oy,
# scale) or richer signature per the composition's needs.
