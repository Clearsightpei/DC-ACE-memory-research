# zhi_know.py — 知 (zhī) — B11 curator promotion (2026-08-03)
# Source: groups/G3_coords/attempts/p3_char_0419_知/generated.py
# Strokes: 8
#
# Recipe: 矢 left + 口 right; fully inline PIL (BANK_DEVIATION from kou.py for thin-uniform consistency)
#
# v8 signature freedom — this file preserves the recipe pointer;
# the full drawing code lives in the source attempt above. To reuse:
#
#   from pathlib import Path
#   ATTEMPT = Path(__file__).parents[2] / "attempts" / "p3_char_0419_知" / "generated.py"
#   exec(ATTEMPT.read_text())
#
# Or copy the draw block into a new function under a fresh (ox, oy,
# scale) or richer signature per the composition's needs.
