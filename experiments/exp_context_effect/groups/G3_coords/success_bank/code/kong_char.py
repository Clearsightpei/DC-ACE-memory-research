# kong_char.py — 空 (kōng) — B11 curator promotion (2026-08-03)
# Source: groups/G3_coords/attempts/p3_char_0397_空/generated.py
# Strokes: 8
#
# Recipe: 宀 top + 八 mid + 工 bottom; fully inline PIL (BANK_DEVIATION — bao_gai_tou/ba/gong nest poorly)
#
# v8 signature freedom — this file preserves the recipe pointer;
# the full drawing code lives in the source attempt above. To reuse:
#
#   from pathlib import Path
#   ATTEMPT = Path(__file__).parents[2] / "attempts" / "p3_char_0397_空" / "generated.py"
#   exec(ATTEMPT.read_text())
#
# Or copy the draw block into a new function under a fresh (ox, oy,
# scale) or richer signature per the composition's needs.
