# Local LLM API client (OpenAI-compatible)
openai>=1.12.0

# Environment variables
python-dotenv>=1.0.0

# Image processing and comparison
Pillow>=10.0.0
scikit-image>=0.21.0
numpy>=1.24.0
opencv-python>=4.8.0

# HTTP client
requests>=2.28.0

# Ollama Python client (for DeepSeek visual comparison)
ollama>=0.4.0

# RapidOCR for fast local Chinese character recognition (rec-only mode)
rapidocr>=3.0.0

# PaddleOCR (optional, for full-pipeline OCR if needed)
paddlepaddle>=2.5.0
paddleocr>=2.7.0

# Trajectory distance metrics
scipy>=1.10.0
dtaidistance>=1.3.0

# System requirements:
# - DeepSeek 32B running locally (via vLLM, Ollama, or similar)
# - Local API endpoint at https://ai-town.mcglobal.ai:9027/v1 (or custom --base-url)
# - ghostscript (install via: brew install ghostscript on Mac)