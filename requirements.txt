# Python dependencies for the exp_context_effect experiment
# (PALM workshop @ NeurIPS 2026 submission)
#
# The drawer subagents were dispatched via the Claude Agent SDK / Claude Code
# CLI — not driven by this requirements.txt. These packages are what the
# drawer scripts (generated.py files under groups/*/attempts/) actually
# import to render PNGs, plus what the tooling scripts use.

# Image rendering (all drawer scripts use PIL)
Pillow>=10.0.0

# Judgment / analysis tooling
numpy>=1.24.0
